<% cancel_path ||= agreement.persisted? ? [:account, agreement] : [:account, @client, :agreements] %>

<%= form_with model: agreement, url: (agreement.persisted? ? [:account, agreement] : [:account, @client, :agreements]), class: 'form' do |form| %>
  <%= render "shared/limits/form", form: form, cancel_path: cancel_path do %>
    <%= render 'account/shared/forms/errors', form: form %>

    <% with_field_settings form: form do %>
      <%= render 'shared/fields/super_select', method: :creator_id, options: {autofocus: true}, html_options: {autofocus: true},
        choices: @agreement.valid_creators.map { |membership| [membership.label_string, membership.id] } %>
      <%= render 'shared/fields/number_field', method: :year %>
      <%= render 'shared/fields/super_select', method: :status %>
      <%# 🚅 super scaffolding will insert new fields above this line. %>
    <% end %>

    <div class="buttons">
      <%= form.submit (form.object.persisted? ? t('.buttons.update') : t('.buttons.create')), class: "button" %>
      <%= link_to t('global.buttons.cancel'), cancel_path, class: "button-secondary" %>
    </div>
  <% end %>
<% end %>
