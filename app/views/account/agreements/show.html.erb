<%= render 'account/shared/page' do |page| %>
  <% page.title t('.section') %>
  <% page.body do %>
    <%= cable_ready_updates_for @agreement do %>
      <%= render 'account/shared/box', divider: true do |box| %>
        <% box.title t('.header') %>
        <% box.description do %>
          <%= t('.description') %>
          <%= t('.manage_description') if can? :manage, @agreement %>
        <% end %>

        <% box.body do %>
          <% with_attribute_settings object: @agreement, strategy: :label do %>
            <%= render 'shared/attributes/belongs_to', attribute: :creator %>
            <%= render 'shared/attributes/number', attribute: :year %>
            <%= render 'shared/attributes/option', attribute: :status %>
            <%# 🚅 super scaffolding will insert new fields above this line. %>
          <% end %>
        <% end %>

        <% box.actions do %>
          <%= link_to t('.buttons.edit'), [:edit, :account, @agreement], class: first_button_primary if can? :edit, @agreement %>
          <%# 🚅 super scaffolding will insert new action model buttons above this line. %>
          <%= button_to t('.buttons.destroy'), [:account, @agreement], method: :delete, class: first_button_primary, data: { turbo_confirm: t('.buttons.confirmations.destroy', model_locales(@agreement)) } if can? :destroy, @agreement %>
          <%= link_to t('global.buttons.back'), [:account, @client, :agreements], class: first_button_primary %>
        <% end %>

        <% box.raw_footer do %>
          <%# 🚅 super scaffolding will insert new action model index views above this line. %>
        <% end %>
      <% end %>
    <% end %>

    <%# 🚅 super scaffolding will insert new children above this line. %>
  <% end %>
<% end %>
