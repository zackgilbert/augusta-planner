en:
  events: &events
    label: &label Events
    breadcrumbs:
      label: *label
    navigation:
      label: *label
      icon: fal fa-puzzle-piece
    buttons: &buttons
      new: Add New Event
      create: Create Event
      edit: Edit Event
      update: Update Event
      destroy: Remove Event
      shorthand:
        edit: Edit
        destroy: Delete
      confirmations:
        # TODO customize for your use-case.
        destroy: Are you sure you want to remove %{event_name}? This will also remove any child resources and can't be undone.
    event:
      buttons: *buttons
    fields: &fields
      id:
        heading: Event ID
      agreement_id:
        heading: Agreement ID
      creator_id: &creator
        _: &creator_id Creator
        label: *creator_id
        heading: *creator_id
        api_title: *creator_id
        api_description: *creator_id
        placeholder: Select
      creator: *creator
      event_type:
        _: &event_type Event Type
        label: *event_type
        heading: *event_type
        api_title: *event_type
        api_description: *event_type
        placeholder: None
        options:
          one: One
          two: Two
          three: Three
      event_date_on:
        _: &event_date_on Event Date On
        label: *event_date_on
        heading: *event_date_on
        api_title: *event_date_on
        api_description: *event_date_on
      market_rate_amount:
        _: &market_rate_amount Market Rate Amount
        label: *market_rate_amount
        heading: *market_rate_amount
        api_title: *market_rate_amount
        api_description: *market_rate_amount
      # 🚅 super scaffolding will insert new fields above this line.
      created_at:
        _: &created_at Added
        label: *created_at
        heading: *created_at
      updated_at:
        _: &updated_at Updated
        label: *updated_at
        heading: *updated_at
    api:
      collection_actions: "Collection Actions for Events"
      index: "List Events"
      create: "Add a New Event"
      member_actions: "Actions for an Individual Event"
      show: "Retrieve a Event"
      update: "Update a Event"
      destroy: "Delete a Event"
      fields: *fields
    index:
      section: "%{agreements_possessive} Events"
      contexts:
        agreement:
          header: Events
          description: Below is a list of Events that have been added for %{agreement_name}.
          description_empty: No Events have been added for %{agreement_name}.
      fields: *fields
      buttons: *buttons
    show:
      section: "%{event_name}"
      header: Event Details
      description: Below are the details we have for %{event_name}.
      manage_description: You'll also find options for updating these details or removing %{event_name} from %{agreement_name} entirely.
      fields: *fields
      buttons: *buttons
    form: &form
      buttons: *buttons
      fields: *fields
    new:
      section: "New Event for %{agreement_name}"
      header: New Event Details
      description: Please provide the details of the new Event you'd like to add to %{agreement_name}.
      form: *form
    edit:
      section: "%{event_name}"
      header: Edit Event Details
      description: You can update the details or settings for %{event_name} below.
      form: *form
    notifications:
      created: Event was successfully created.
      updated: Event was successfully updated.
      destroyed: Event was successfully destroyed.
  account:
    events: *events
  activerecord:
    attributes:
      event:
        creator_id: *creator_id
        event_type: *event_type
        event_date_on: *event_date_on
        market_rate_amount: *market_rate_amount
        # 🚅 super scaffolding will insert new activerecord attributes above this line.
        created_at: *created_at
        updated_at: *updated_at
